apiVersion: v1
kind: Service
metadata:
  name: my-app-service
spec:
  selector:
    app: youtube-app-deployment
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
  type: LoadBalancer
  sessionAffinity: None
